require 'spec_helper.rb'

describe Hue::CLI do

  context 'when executed without any parameters' do
    it 'should print the bridge state' do
      # Hue::Bridge.any_instance.stubs(:print_state).once.returns(nil)
      # mock_bridge
      # Hue::CLI.run
    end
  end

end
